#pragma once
#include <string>
#include <vector>

// Function to read the entire input file as a single string
std::string read_input(int year, int day);

// Function to read the input file line by line into a vector of strings
std::vector<std::string> read_input_lines(int year, int day);
